using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WaveCountStarter : MonoBehaviour
{
    public WaveCounter data;
    // Start is called before the first frame update
    void Start()
    {
        data.waveCounter = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
